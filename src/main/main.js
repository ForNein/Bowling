function calculateScore(series) {
	var score;
	return score;
}

function handleStrike(series, index) {
	var score;
	return score;
}

function calculateScore(series, index) {
	var score;
	return score;
}